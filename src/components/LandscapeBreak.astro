<section class="landscape-break" aria-hidden="true">
	<img
		src={`${import.meta.env.BASE_URL}/images/landscape-nordic-noir.jpg`}
		alt="Nordic coastal landscape"
		class="landscape-break-img"
		loading="eager"
		decoding="async"
		width="1920"
		height="1080"
	/>
</section>

<style>
	.landscape-break {
		position: relative;
		width: 100%;
		height: 60vh;
		overflow: hidden;
		margin-top: -24px; /* pulls image up under hero for seamless transition */
	}

	/* Dark overlay */
	.landscape-break::after {
		content: '';
		position: absolute;
		inset: 0;
		background: linear-gradient(
			180deg,
			rgba(15, 46, 36, 0.28) 0%,
			rgba(15, 46, 36, 0.22) 50%,
			rgba(15, 46, 36, 0.32) 100%
		);
		pointer-events: none;
	}

	/* Subtle vignette */
	.landscape-break::before {
		content: '';
		position: absolute;
		inset: 0;
		box-shadow: inset 0 0 120px 40px rgba(15, 46, 36, 0.15);
		z-index: 1;
		pointer-events: none;
	}

	.landscape-break-img {
		width: 100%;
		height: 100%;
		object-fit: cover;
		object-position: center;
		display: block;
		filter: saturate(0.72) contrast(0.85) brightness(0.91);
	}

	@media (max-width: 768px) {
		.landscape-break {
			height: 42vh;
			margin-top: -12px;
		}
	}
</style>
